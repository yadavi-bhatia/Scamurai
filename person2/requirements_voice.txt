# Core dependencies for Voice Agent
numpy>=1.24.0
librosa>=0.10.0
soundfile>=0.12.0

# Optional: Better voice encoding (recommended for production)
resemblyzer>=0.1.3

# For HTTP server mode (included in Python standard library)
# No additional dependencies needed

# For audio processing
scipy>=1.10.0